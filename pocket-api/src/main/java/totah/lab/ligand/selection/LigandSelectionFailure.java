package totah.lab.ligand.selection;

public enum LigandSelectionFailure {
    AMBIGUOUS_SELECTION,
    SELECTION_NOT_FOUND,
    NOT_EXTRACTED_AS_LIGAND,
    UNSUPPORTED_CLASSIFICATION,
    EXCLUDED_BY_POLICY
}
