package totah.lab.prometheus.potential.delta.environment;

/** Fixed graph-derived chemical types; no atom identity or QM target enters typing. */
public enum SpeciesChannel { C_SP3, C_SP2, C_CARBONYL, O_CARBONYL, O_ETHER, S_THIOL, H_C, H_S }
