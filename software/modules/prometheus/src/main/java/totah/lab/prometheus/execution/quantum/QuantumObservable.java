package totah.lab.prometheus.execution.quantum;

/** Scientifically typed quantities that a quantum backend may produce. */
public enum QuantumObservable {
    ABSOLUTE_ENERGY,
    CARTESIAN_GRADIENT,
    CARTESIAN_FORCE,
    HESSIAN,
    ELECTROSTATIC_POTENTIAL
}
