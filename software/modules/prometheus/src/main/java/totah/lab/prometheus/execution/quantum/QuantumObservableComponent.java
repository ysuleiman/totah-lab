package totah.lab.prometheus.execution.quantum;

/** A separately executable and persistable component of a quantum observable. */
public enum QuantumObservableComponent {
    ELECTRONIC,
    DISPERSION,
    TOTAL
}
