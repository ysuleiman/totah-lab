package totah.lab.prometheus.store;

/** Machine-readable class of a remembered generation failure. */
public enum GeneratedFailureClassification {
    EXECUTION_FAILED,
    RESULT_VALIDATION_FAILED,
    REGISTRATION_FAILED
}
