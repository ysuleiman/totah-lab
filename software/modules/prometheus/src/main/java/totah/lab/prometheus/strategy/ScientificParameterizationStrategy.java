package totah.lab.prometheus.strategy;

/** Supplies a scientific methodology declaration without implying an installed executor. */
public interface ScientificParameterizationStrategy {
    ScientificStrategyDescriptor scientificDescriptor();
}
