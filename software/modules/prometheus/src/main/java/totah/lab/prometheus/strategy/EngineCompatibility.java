package totah.lab.prometheus.strategy;

/** Declared compatibility of a methodology's production functional form with an MD engine family. */
public enum EngineCompatibility {
    DIRECT,
    REQUIRES_DOCUMENTED_TRANSLATION,
    DEPENDS_ON_SELECTED_FUNCTIONAL_FORM,
    NOT_SUPPORTED
}
