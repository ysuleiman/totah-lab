package totah.lab.prometheus.comparison;

/** Scientific dimensions that remain independent throughout model comparison. */
public enum MetricDimension {
    ENERGY,
    FORCE,
    GEOMETRY,
    ELECTROSTATICS,
    CONFORMATION,
    INTERMOLECULAR_INTERACTION,
    STABILITY,
    IMPLEMENTATION_COMPATIBILITY
}
