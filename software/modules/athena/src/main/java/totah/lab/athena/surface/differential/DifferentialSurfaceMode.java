package totah.lab.athena.surface.differential;

/** Identifies the scientific definition used to produce a surface map. */
public enum DifferentialSurfaceMode {
    SURFDIFF_COMPATIBLE,
    ATHENA_DIFFERENTIAL_SURFACE
}
