package totah.lab.athena.surface.differential;

/** Direction of a differential map; no affinity interpretation is implied. */
public enum DifferentialDirection {
    QUERY_VS_SUBJECT,
    SUBJECT_VS_QUERY
}
