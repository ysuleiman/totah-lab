package totah.lab.hermes.structure;

/**
 * Stable failure categories exposed by structure resolution.
 */
public enum StructureResolutionFailure {
    PROTEIN_NOT_FOUND,
    PROVIDER_FAILURE,
    INVALID_PROVIDER_RESPONSE
}
