package totah.lab.web.persistence;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PoseResidueContactRepository
        extends JpaRepository<PoseResidueContactEntity,
                PoseResidueContactId> {
}
